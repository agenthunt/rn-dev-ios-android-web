import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('NavigationSetup', () => App);
AppRegistry.runApplication('NavigationSetup', {
  rootTag: document.getElementById('root')
});
